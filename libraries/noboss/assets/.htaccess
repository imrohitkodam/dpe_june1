# Apache 2.4+
<IfModule mod_authz_core.c>
  Require all granted
</IfModule>

# Apache 2.0-2.2
<IfModule !mod_authz_core.c>
  Allow from all
</IfModule>

<IfModule mod_rewrite.c>
	RewriteEngine On
	RewriteRule ^(.*)$ - [L]
</IfModule>